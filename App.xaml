<Application
    x:Class="StudentEMS.App"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:StudentEMS"
    xmlns:viewModels="clr-namespace:StudentEMS.ViewModels"
    xmlns:views="clr-namespace:StudentEMS.Views">

    <Application.Resources>

        <DataTemplate DataType="{x:Type viewModels:HomeViewModel}">
            <views:HomeView />
        </DataTemplate>

        <DataTemplate DataType="{x:Type viewModels:UpdateProfileViewModel}">
            <views:UpdateProfileView />
        </DataTemplate>

        <DataTemplate DataType="{x:Type viewModels:SubjectViewModel}">
            <views:SubjectView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewModels:StudentHomeViewModel}">
            <views:StudentHomeView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewModels:LoginViewModel}">
            <views:LoginView />
        </DataTemplate>

    </Application.Resources>
</Application>
